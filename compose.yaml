services:
  server:
    build:
      context: .
    env_file:
      - .env
    environment:
      NODE_ENV: production
      CORS_ORIGINS: http://localhost:4200
    ports:
      - "${PORT:-3000}:${PORT:-3000}"
    restart: unless-stopped
